import { Link } from 'react-router-dom';
import { Button } from 'tackle-react';

const HomePage = () => {
   return (
      <div>
         <Link to="/browse">Browse</Link>
      </div>
   );
}

export default HomePage;
